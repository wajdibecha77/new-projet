{
  "buildCommand": "npm install -g @angular/cli && npm install && NODE_OPTIONS=--openssl-legacy-provider ng build --configuration production",
  "outputDirectory": "dist/frontend-pfe-main",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}